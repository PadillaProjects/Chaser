rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Basic rule: User can read/write if authenticated
    // TODO: Tighten this up based on database-schema.mdx rules
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
